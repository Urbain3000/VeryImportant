package objet;

public class Objects {

	public static void main(String[] args) {
		// an object can be related to reel world object ( like cars, Bible, dog, house, laptop, pc, anithings)
		// an object is an instance of a class
		// an object has a state = color, name, age, person etc  "les attributs" en fait ce sont les variables qui vont constituer l'objet
		//Cars= model, color, number, height, width, 
		
		
		//Behaviour Comportement
		// the object person for example can have a behaviour. In fact object can have behaviours. Le comportement ce sont les METHODES
		// the object car can also have a behavior (like standing of the car, break, running, stoping etc
		
		// example d'objet:
		
		// Un Robot        robot spectrum = new robot();
		// un Homme        femme nom = new nom();
		Fan fanObject1 = new Fan();
		femme femmeObjet = new femme();
		
		
		comment acceder à l'objet
		

	}

}
